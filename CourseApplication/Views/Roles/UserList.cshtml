@model IEnumerable<CourseApplication.Models.User>

    <h2>User roles control</h2>
    <table class="table">
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.Email</td>
                <td>
                    <a class="btn btn-primary" asp-action="EditRoles" asp-route-userid="@user.Id">Access Rights</a>
                </td>
            </tr>
        }
    </table>
